wsl --install
sudo apt update && sudo apt -y upgrade
sudo apt -y install python3 python3-venv python3-pip build-essential
python3 -m venv .venv
source .venv/bin/activate
pip install -U "pathway[xpack-llm]"
